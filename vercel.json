{
  "version": 2,
  "builds": [
    {
      "src": "src/app/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/src/app/api/$1"
    }
  ],
  "buildCommand": "npm run build --force",
  "outputDirectory": "dist"
}
